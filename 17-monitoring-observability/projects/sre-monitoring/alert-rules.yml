groups:
  - name: sre-monitoring
    rules:
      - alert: PrometheusTargetDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Prometheus target is down"
          description: "A configured Prometheus target has been unavailable for more than one minute."
